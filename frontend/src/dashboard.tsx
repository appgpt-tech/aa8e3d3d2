import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "projectmanagementapp" />
      <CardContent> A Project Management Application to manage tasks and teams < /CardContent>
      </Card>
    );