import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "task management app" />
      <CardContent> This app helps users to manage their tasks efficiently. The users can perform all the essential tasks and track their progress. < /CardContent>
      </Card>
    );